# Checkpoint 1 review

Review completed on 13 September 2026 before committing the checkpoint.

## Synthetic company findings

The Atlas decision chain is coherent: an operational deadline creates a hard delivery constraint; a current evaluation establishes mandatory compliance; a supplier comparison explains the relative outcome; a revised quotation closes the commercial gap; and an approved Gate 3 record captures the decision. The confidential evaluated cost breakdown is plausibly shared with Finance and Procurement but is not required to substantiate the public rationale.

The superseded lineage creates a credible conflict rather than a planted contradiction. Version 2 prefers Meridian while Nova lacks a confirmed delivery date; version 3 records Nova's technical lead while withholding an award recommendation; version 4 follows the delivery confirmation and revised offer. Current-answer assembly excludes v2 and v3 but keeps them inspectable as history for identities that can access the family.

The initial background corpus did not pass the believability gate: its repeated “record 001” titles and self-describing synthetic passages read as generator output. It was replaced with department-owned operational reports, design records, supplier controls, finance records, safety actions, people plans, and governance memoranda. Synthetic status is now disclosed once at the organization/UI level rather than written into every record body.

## Identity findings

- General Employee can see the published decision minute and delivery requirement, but not the evaluation family, final comparison, quotation, or confidential costs. It refuses a complete rationale.
- Operations Manager is on the Atlas core team and can inspect the current evaluation and its history alongside operational evidence. Procurement-only comparison and quotation records remain concealed, so the commercial claim is omitted and the result is partial.
- Procurement Officer can see operationally published material, Atlas core material, Procurement evidence, and the Finance/Procurement cost breakdown. The latter is a candidate but not cited because it is unnecessary to support the four answer claims.

Procurement no longer inherits the entire Finance group. The one jointly controlled cost record grants both groups explicitly.

## Retrieval findings

Visible results expose supported claims, citations, ordinal source rank, excluded-version metadata, concealed restriction counts, and named system operations. Numeric candidate weighting remains an internal deterministic implementation detail and is removed from the public result shape; it cannot be presented as model confidence. Restricted exclusions expose neither source title nor source identifier. Unsupported questions produce no evidence and a direct refusal.

## Architecture findings

The physical and documented boundaries separate data, knowledge behavior, graphics capability/renderer work, themes, mechanics-only shared code, direction-owned design trees, compare, and lead capture. Direction folders contain only checkpoint markers. Shared UI is explicitly prohibited from defining page grids, cards, headers, typography scales, or animation grammar.

## Dependency findings

The proposed version floors were rechecked against npm registry metadata. Vite 8.3 and the React plugin share compatible Node requirements; plugin-react 6.1 accepts Vite 8; React DOM 19.3 requires React 19.3; and the Three.js, GSAP, TypeScript, and Playwright proposals are published. Semver ranges are proposals until Checkpoint 2 resolves a lockfile and runs the actual build/type-check.
