# Institutional OS

This direction owns the operational knowledge workspace at `/os`: its topbar, domain rail, command surface, answer ledger, exclusions, source inspector, deterministic trace, typography, layout, and responsive rules.

It consumes the shared synthetic model, retrieval function, theme state, route links, and query-state hook. No Institutional OS composition is exported as a shared visual primitive.
